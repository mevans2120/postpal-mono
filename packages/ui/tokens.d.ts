/**
 * Types for tokens.js (the runtime palette). Keys are enumerated so component
 * accesses (colors.paper, colors['clay-deep'], …) are key-checked.
 */
export declare const colors: {
  readonly paper: string;
  readonly card: string;
  readonly ink: string;
  readonly 'ink-soft': string;
  readonly mut: string;
  readonly 'mut-soft': string;
  readonly clay: string;
  readonly 'clay-fill': string;
  readonly 'clay-deep': string;
  readonly 'clay-soft': string;
  readonly 'clay-soft-line': string;
  readonly pine: string;
  readonly 'pine-soft': string;
  readonly line: string;
  readonly alert: string;
};
